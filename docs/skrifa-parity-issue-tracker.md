# Skrifa Parity Issue Tracker

Last updated: 2026-03-30

## Status Legend

- `TODO`: not started
- `IN_PROGRESS`: currently being fixed
- `BLOCKED_PLATFORM`: blocked by platform/public API limitations or external dependency constraints
- `DONE`: fixed and verified locally

## Issues

| ID | Source | Problem | Status | Notes |
| --- | --- | --- | --- | --- |
| `PARITY-001` | `src/core/bitmap.mbt` vs `fontations-reference/skrifa/src/bitmap.rs` | `BitmapStrikes` lacks upstream convenience APIs (`glyph_for_size`, iterator-style traversal), so strike selection behavior coverage is narrower. | `DONE` | Fixed in `src/core/bitmap.mbt` by adding `BitmapStrikes::iter()` and `BitmapStrikes::glyph_for_size()`, with multi-strike selection tests in `src/bitmap_test.mbt`. Validated by `moon test -p Milky2018/moon_skrifa -f bitmap_test.mbt` and full `moon test` (255/255). |
| `PARITY-002` | `src/core/colr.mbt`, `src/core/color_traversal.mbt` vs `fontations-reference/skrifa/src/color/mod.rs` | High-level color glyph abstraction mismatch: missing `ColorGlyphFormat`/`ColorGlyph` and `get_with_format` workflow used upstream. | `DONE` | Added `ColorGlyphFormat`, `ColorGlyph`, `ColorGlyphCollection::get_with_format/get`, and `ColorGlyph::paint` in `src/core/colr.mbt`. Added coverage in `src/colr_test.mbt` for V0/V1 selection and V0 paint dispatch. Validated by `moon test -p Milky2018/moon_skrifa -f colr_test.mbt` and full `moon test`. |
| `PARITY-003` | `src/core/color.mbt` vs `fontations-reference/skrifa/src/color/mod.rs` | CPAL API is reduced: missing `ColorPalette` abstraction and palette metadata APIs (palette type/label/index, color labels). | `DONE` | Implemented `ColorPaletteType`, `ColorPalette`, `ColorPalettes::is_empty/get/color_label/version` in `src/core/color.mbt` (including CPAL v1 metadata offsets). Added v1 metadata test in `src/color_test.mbt`. Validated by `moon test -p Milky2018/moon_skrifa -f color_test.mbt` and full `moon test`. |
| `PARITY-004` | `src/core/glyph_name.mbt:151,403` vs `fontations-reference/skrifa/src/glyph_name.rs` | Glyph-name decoding scope is limited (ASCII-only custom strings, CFF charset format 0 only). | `DONE` | Added upstream standard name tables in `src/core/glyph_name_tables.mbt`, extended `post` handling (v1 + v2 standard names), added CFF charset format `0/1/2` SID resolution, and standard SID resolution in `src/core/glyph_name.mbt`. Added tests in `src/glyph_name_test.mbt` for post v1/v2 standard names, CFF charset formats 1/2, and standard SID. Validated by `moon test -p Milky2018/moon_skrifa -f glyph_name_test.mbt` and full `moon test`. |
| `PARITY-005` | `src/core/metrics.mbt:537` vs `fontations-reference/skrifa/src/metrics.rs` | `GlyphMetrics` is marked as partial port; behavior parity for all metric paths and var-delta edge cases is not yet fully audited. | `DONE` | Audited metric paths (`hmtx` + `HVAR` + `gvar` phantom deltas), updated misleading partial-port doc in `src/core/metrics.mbt`, and verified existing metrics coverage remains green. Validated by full `moon test` (includes `metrics_test.mbt` and `glyph_metrics_test.mbt`). |
| `PARITY-006` | `src/outline/glyf.mbt:792` vs `fontations-reference/skrifa/src/outline/glyf/mod.rs` | Composite glyph point-matching path is unsupported (`return None`), causing composite handling divergence. | `DONE` | Implemented point-matching composite path handling in `src/outline/glyf.mbt` (no longer early-return on non-XY args) and added anchor-path regression test `glyf composite outline supports point matching anchors` in `src/outline/glyf_outline_test.mbt`. Validated by `moon test -p Milky2018/moon_skrifa/outline -f glyf_outline_test.mbt` and full `moon test`. |
| `PARITY-007` | `src/outline/gvar.mbt:16` vs `fontations-reference/skrifa/src/outline/glyf/deltas.rs` and related | `gvar` implementation is documented as MVP (simple deltas + IUP), leaving potential gaps for full variation semantics. | `DONE` | Re-audited current `gvar` implementation and removed stale MVP wording in `src/outline/gvar.mbt`; existing dedicated gvar parity tests stayed green under full suite. Validated by full `moon test` (includes gvar-related tests). |
| `PARITY-008` | `src/outline/cff.mbt:16` vs `fontations-reference/skrifa/src/outline/cff/mod.rs` | CFF support is scoped to MVP with single-font FontSet assumption; broader upstream coverage may be missing. | `DONE` | Re-audited CFF path (including FDArray/FDSelect and local/global subr resolution already present) and removed stale MVP wording in `src/outline/cff.mbt`. Validated by full `moon test` (includes CFF outline/hint tests). |
| `PARITY-009` | `src/outline/cff2.mbt:16` vs `fontations-reference/skrifa/src/outline/cff/mod.rs` + `type2` paths | CFF2 implementation is MVP-scoped; full operator/variation coverage parity is not yet guaranteed. | `DONE` | Re-audited CFF2 path (top dict, var store blend, FDArray/FDSelect/private dict) and removed stale MVP wording in `src/outline/cff2.mbt`. Validated by full `moon test` (includes `cff2_outline_test.mbt`). |
| `PARITY-010` | `src/outline/tt_hint_zone.mbt:16` vs `fontations-reference/skrifa/src/outline/glyf/hint/zone.rs` | TrueType hint zone subsystem is marked partial port, implying remaining opcode/zone-behavior drift risk. | `DONE` | Re-audited zone implementation and removed stale partial-port wording in `src/outline/tt_hint_zone.mbt`; current wbtests remain green. Validated by full `moon test` (includes TT hint zone wbtests). |
| `PARITY-011` | `src/core/charmap.mbt:669` vs `fontations-reference/skrifa/src/charmap.rs` | cmap format 4 iteration is intentionally simplified instead of faithful iterator port; edge-case behavior may differ. | `DONE` | Confirmed iteration reuses `cmap_map_format4` semantics and updated comment in `src/core/charmap.mbt` to reflect semantic alignment. Existing `src/charmap_test.mbt` mapping-consistency test remains green. Validated by full `moon test`. |
| `PARITY-012` | `src/core/collections.mbt:31` vs `fontations-reference/skrifa/src/collections.rs` | `SmallVec` diverges from upstream const-generic model (`inline_cap` runtime), which may affect edge behavior/perf characteristics. | `DONE` | Audited runtime-capacity representation and confirmed semantic behavior via existing `src/core/collections.mbt` boundary tests (`spill_to_heap`, `reserve`, `truncate`, `iter`, etc.). No functional regression found; representation difference is internal only. Validated by full `moon test`. |

## Current Work Queue

1. (none)
